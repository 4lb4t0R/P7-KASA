import logo from '../assets/LOGO.png'

function Header () {
return(
    <div>
        <a href={logo}/>
    <nav>
        <ul>
            <li></li>
            <li></li>
        </ul>
    </nav>
    </div>
)
}

export default Header